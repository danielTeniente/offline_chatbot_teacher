## ocr_utils.py

This project extracts text from PDF books using OCR and allows phrase-based search across scanned pages.

### Features

- Converts PDF pages to high-resolution images.
- Detects text regions using OpenCV.
- Extracts text using Tesseract OCR.
- Caches scanned text for faster future searches.
- Searches for phrases and returns contextual matches.

